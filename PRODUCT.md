# Product

## Register

product

## Users

主要用户是长时间使用 SillyTavern 进行角色扮演、创作或持续对话，并积累了大量聊天楼层的用户。他们通常在桌面或移动浏览器中连续使用聊天界面，希望在保留完整历史记录的同时，减少页面卡顿、上下文延迟和重复配置。

用户的核心任务是在不打断正常聊天的情况下，让长对话保持流畅，并能清楚了解摘要状态、失败原因和当前采用的上下文策略。

## Product Purpose

Smooth Chat 使用 SillyTavern 原生消息加载上限、增量摘要和请求期上下文管理，让超长聊天的浏览器渲染量与模型请求上下文保持在可控范围内。

产品成功意味着：

- 长聊天仍能顺畅滚动、输入和切换。
- 生成延迟不会因全部旧楼层持续增长。
- 原始聊天记录永不被插件删除。
- 摘要失败或过期时自动安全降级，不影响正常回复。
- 用户无需理解复杂的上下文工程也能掌握插件当前状态。

## Brand Personality

克制、可靠、轻量。

文案直接说明当前状态和操作后果，不夸大性能效果，不隐藏摘要的不确定性。界面应像 SillyTavern 自带功能的一部分，而不是独立品牌或营销页面。

## Anti-references

- 不做充满装饰渐变、发光效果和大面积品牌色的“性能仪表盘”。
- 不使用复杂嵌套卡片、夸张圆角或大量无意义动效。
- 不把正常运行状态包装成焦虑性的红黄告警。
- 不模仿系统清理软件，以“加速百分比”“释放内存”等无法证明的数据误导用户。
- 不隐藏摘要生成的额外 token 消耗或模型可能遗漏信息的风险。

## Design Principles

1. **融入宿主**：复用 SillyTavern 的主题变量、控件和交互习惯。
2. **安全优先**：只有摘要有效且状态一致时才裁剪请求；任何异常都回退到完整上下文。
3. **原始记录不可损**：性能优化只作用于渲染窗口和单次请求，不删除聊天历史。
4. **状态可解释**：用明确文字说明已总结范围、下次触发时机和错误原因。
5. **高级能力渐进呈现**：默认值开箱即用，重建、恢复和摘要编辑等操作按需出现。
6. **不改用户主题**：不修改聊天气泡、字体、颜色、背景、模糊、阴影或布局风格。

## Accessibility & Inclusion

- 以 WCAG 2.1 AA 为最低目标。
- 所有表单和按钮支持键盘操作并具有可读标签。
- 状态不能只通过颜色表达，必须同时提供文本或图标含义。
- 尊重 `prefers-reduced-motion`，不依赖动画传达状态。
- 使用 SillyTavern 主题文本和背景变量，保持明暗主题可读性。
- 移动端不出现横向滚动，触控目标保持足够尺寸。
